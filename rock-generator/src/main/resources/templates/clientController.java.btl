package ${controllerClientPackage};

import org.springframework.cloud.openfeign.FeignClient;

import com.lzb.rock.base.config.FeignClientCfg;

import ${controllerfacadePackage}.${entity}Facade;

/**
 * <p>
 * ${table.comment!} client 接口
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
@FeignClient(name= ${bizEnBigName}Facade.SERVICE_NAME,configuration = FeignClientCfg.class)
public interface ${entity}Client extends ${entity}Facade{
}
